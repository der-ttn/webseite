apiVersion: v1
kind: Service
metadata:
  name: {{ include "ttn-web.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ttn-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ttn-web.selectorLabels" . | nindent 4 }}
